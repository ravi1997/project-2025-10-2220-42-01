#include "../include/optimizers.hpp"
#include <algorithm>
#include <cmath>
#include <numeric>

namespace dnn {

// Optimizer implementations are header-only since they're template-heavy
// This file exists to satisfy the CMakeLists.txt but can be empty
// All implementation is in the header file

} // namespace dnn